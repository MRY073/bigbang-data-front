# 后台开发提示词：商品表新增提示词备注字段

## 需求概述

在商品表（product_items）中新增一个 `prompt_note`（提示词备注）字段，用于存储商品的提示词备注信息。该字段为可选字段，支持最多2000个字符的文本内容。

## 数据库变更

### 1. 数据库迁移脚本

需要在商品表（product_items）中新增字段：

```sql
ALTER TABLE product_items 
ADD COLUMN prompt_note TEXT NULL COMMENT '提示词备注，最多2000字符';
```

或者如果使用迁移工具（如 Laravel migrations、Django migrations 等），请创建相应的迁移文件。

### 2. 字段规格

- **字段名**: `prompt_note`
- **数据类型**: `TEXT` 或 `VARCHAR(2000)`（根据数据库类型选择）
- **是否必填**: 否（允许 NULL）
- **默认值**: NULL
- **字符限制**: 最多 2000 个字符
- **说明**: 用于存储商品的提示词备注信息

## API 接口变更

### 1. 更新商品接口（PUT /api/product-items/:id）

**现有接口**: `PUT /api/product-items/:id`

**请求体变更**: 在现有的 `updateProductItem` 接口中，需要支持 `prompt_note` 字段的更新。

**请求示例**:
```json
{
  "prompt_note": "这是商品的提示词备注内容，可以包含详细的说明信息..."
}
```

**响应格式**: 保持现有响应格式不变，但需要在返回的商品数据中包含 `prompt_note` 字段。

**验证规则**:
- `prompt_note` 字段为可选
- 如果提供，必须是字符串类型
- 字符串长度不能超过 2000 个字符
- 允许空字符串，保存时应转换为 NULL

### 2. 获取商品列表接口（GET /api/product-items）

**现有接口**: `GET /api/product-items`

**响应变更**: 在返回的商品数据中，需要包含 `prompt_note` 字段。

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "product_id": "12345",
      "product_name": "商品名称",
      "product_image": "https://example.com/image.jpg",
      "custom_category_1": "分类1",
      "custom_category_2": "分类2",
      "custom_category_3": "分类3",
      "custom_category_4": "分类4",
      "prompt_note": "这是提示词备注内容...",
      ...
    }
  ],
  "total": 100
}
```

### 3. 获取单个商品接口（GET /api/product-items/:id）

**现有接口**: `GET /api/product-items/:id`

**响应变更**: 在返回的商品数据中，需要包含 `prompt_note` 字段。

### 4. 其他相关接口

以下接口也需要在返回数据中包含 `prompt_note` 字段：
- `GET /api/product-items/offline` (获取已下架商品列表)
- 其他返回商品数据的接口

## 数据验证

### 后端验证规则

1. **类型验证**: `prompt_note` 必须是字符串类型或 null
2. **长度验证**: 如果提供值，字符串长度不能超过 2000 个字符
3. **空值处理**: 
   - 空字符串 `""` 应转换为 `NULL` 存储
   - `null` 值直接存储为 `NULL`
   - 只包含空白字符的字符串应去除首尾空格后，如果为空则转换为 `NULL`

### 验证示例（伪代码）

```javascript
// 示例验证逻辑
function validatePromptNote(value) {
  if (value === null || value === undefined) {
    return null;
  }
  
  if (typeof value !== 'string') {
    throw new Error('prompt_note must be a string');
  }
  
  const trimmed = value.trim();
  
  if (trimmed.length === 0) {
    return null;
  }
  
  if (trimmed.length > 2000) {
    throw new Error('prompt_note cannot exceed 2000 characters');
  }
  
  return trimmed;
}
```

## 实现步骤

1. **创建数据库迁移**
   - 在商品表中添加 `prompt_note` 字段
   - 执行迁移脚本

2. **更新数据模型**
   - 在商品模型（Model）中添加 `prompt_note` 字段
   - 设置字段类型、验证规则等

3. **更新 API 接口**
   - 在更新商品接口中添加 `prompt_note` 字段的处理
   - 在获取商品接口的返回数据中包含 `prompt_note` 字段
   - 添加字段验证逻辑

4. **更新 API 文档**
   - 更新相关接口文档，说明新增字段

5. **测试**
   - 测试字段的创建、更新、查询功能
   - 测试字段验证（长度限制、类型验证等）
   - 测试空值处理

## 注意事项

1. **向后兼容**: 该字段为可选字段，不应影响现有功能
2. **性能考虑**: 由于是 TEXT 类型字段，查询时注意性能影响
3. **数据迁移**: 如果已有数据，新字段默认为 NULL，无需数据迁移
4. **字符编码**: 确保数据库和 API 使用 UTF-8 编码，以支持各种字符

## 前端对接说明

前端已经完成以下实现：
- 在 `ProductItem` 接口中添加了 `prompt_note?: string | null` 字段
- 在表格中新增了"提示词备注"列
- 实现了点击弹窗编辑功能
- 实现了保存功能，使用 `PUT /api/product-items/:id` 接口
- 前端已实现 2000 字符限制和显示省略号功能

**前端请求示例**:
```typescript
// 更新提示词备注
PUT /api/product-items/123
{
  "prompt_note": "这是提示词备注内容..."
}
```

## 测试用例

### 1. 正常更新提示词备注
- **请求**: `PUT /api/product-items/1` with `{"prompt_note": "测试备注"}`
- **预期**: 返回成功，数据库中字段已更新

### 2. 更新为空字符串
- **请求**: `PUT /api/product-items/1` with `{"prompt_note": ""}`
- **预期**: 返回成功，数据库中字段为 NULL

### 3. 更新为 null
- **请求**: `PUT /api/product-items/1` with `{"prompt_note": null}`
- **预期**: 返回成功，数据库中字段为 NULL

### 4. 超过长度限制
- **请求**: `PUT /api/product-items/1` with `{"prompt_note": "超过2000字符的字符串..."}`
- **预期**: 返回错误，提示长度超过限制

### 5. 获取商品列表包含 prompt_note
- **请求**: `GET /api/product-items`
- **预期**: 返回的商品数据中包含 `prompt_note` 字段

## 完成标准

- [ ] 数据库迁移完成，字段已添加
- [ ] 数据模型已更新
- [ ] 更新商品接口支持 `prompt_note` 字段
- [ ] 获取商品接口返回数据包含 `prompt_note` 字段
- [ ] 字段验证逻辑已实现（长度、类型）
- [ ] 空值处理正确（空字符串转 NULL）
- [ ] 所有相关接口已更新
- [ ] 测试通过
- [ ] API 文档已更新

